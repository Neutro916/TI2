services:
  - type: web
    name: t2i-terminal
    env: docker
    plan: starter
    region: oregon 
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ADMIN_PASSWORD
        generateValue: true
